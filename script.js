/*
func add(a, b)
    return a + b

func subtract(a, b)
    return a - b

func multiply(a, b)
    return a * b

func divide(a, b)
    return a / b

func operate(operator, a, b):
    switch operator:
        +:
            add(a, b)
        -:
            subtract(a, b)
        *:
            multiply(a, b)
        /:
            divide(a, b)
        def:
            throw unknown

func onClick():
    switch:
        operator:
            operate();
        number:
        clear:
        backspace:



*/